<%= form_for(@attribute_reference) do |f| %>
  <% if @attribute_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@attribute_reference.errors.count, "error") %> prohibited this attribute_reference from being saved:</h2>

      <ul>
      <% @attribute_reference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
